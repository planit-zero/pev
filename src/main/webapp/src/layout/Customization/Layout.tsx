// material-ui
import { useTheme } from '@mui/material/styles';
import { FormControl, FormControlLabel, FormLabel, Radio, RadioGroup, Switch, PaletteMode, useMediaQuery } from '@mui/material';

// project imports
import useConfig from 'hooks/useConfig';
import LAYOUT_CONST from 'constant';

const Layout = () => {
    return null;
};

export default Layout;
